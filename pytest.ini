[pytest]
# Tests del repo principal viven en tests/. Los SDKs (sdk/python, sdk/typescript)
# tienen sus propios tests con su pytest.ini propio y se corren desde cd sdk/<lang>.
testpaths = tests
norecursedirs = sdk infra .venv .git node_modules
