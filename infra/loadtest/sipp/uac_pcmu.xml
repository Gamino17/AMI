<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Escenario SIPp UAC: coloca llamadas contra Asterisk ofreciendo PCMU (G.711 u-law,
  8kHz, ptime 20) y reproduce audio RTP real desde un pcap -> ejercita el camino de
  media completo (trunk leg + Stasis -> bridge -> mock agent).

  Requiere un pcap de audio u-law en infra/loadtest/sipp/pcmu.pcap
  (los ejemplos de SIPp traen g711u.pcap / pcmu.pcap; cópialo aquí).

  Lanzar (ver run.sh):
    sipp <ASTERISK_IP>:5060 -sf uac_pcmu.xml -s <NUMERO_MID_TEST> \
         -l <max_concurrentes> -r <rate/s> -d <duracion_ms> -mi <IP_local> \
         -trace_err -m <total>
-->
<scenario name="AMI uac PCMU media">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=ptime:20
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Reproduce audio u-law real por RTP (la clave para estresar el bridge). -->
  <nop>
    <action>
      <exec play_pcap_audio="sipp/pcmu.pcap"/>
    </action>
  </nop>

  <!-- Duración de la llamada (ms). Sobrescribible con -d en run.sh. -->
  <pause milliseconds="30000"/>

  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="10, 50, 100, 200, 500, 1000, 2000"/>
  <CallLengthRepartition   value="10000, 20000, 30000, 40000, 60000"/>
</scenario>
